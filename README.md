## Job Finder

Backend app built with SpringBoot to help job seeker find their dream job.

# This app built with
- Postgres as a Database
- lombok to avoid boilerplate code for setter and getters for the model
- JPA and hibernate for ORM "Object Releational Mapping"
